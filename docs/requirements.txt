# Minimal documentation requirements for ReadTheDocs
# Only essential packages for building Sphinx documentation

# Core Sphinx and extensions
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0
sphinx-autodoc-typehints>=1.12.0
sphinx-copybutton>=0.4.0
sphinx-tabs>=3.0.0

# Math rendering support
sphinx.ext.mathjax
sphinx.ext.imgmath

# For API documentation (lightweight core packages)
numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0
scikit-learn>=1.0.0

# Note: All heavy dependencies (torch, jax, numba, etc.) are mocked in conf.py
# This prevents installation issues on ReadTheDocs while still allowing
# documentation to be generated with proper type hints and imports.

# Additional documentation tools for enhanced ML/neural network docs
sphinx-autodoc-typehints>=1.12.0
sphinx-copybutton>=0.4.0
sphinx-tabs>=3.0.0
sphinx-rtd-theme>=1.0.0

# For better type hint handling in enhanced estimators
typing-extensions>=4.0.0
